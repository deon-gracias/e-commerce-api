const { getAllUsers } = require("../users.controller.js");
const request = require("supertest");

describe("Unit Testing Users", () => {
  it("should get all users", () => {});

  it("should get user by id", () => {});

  it("should sign up", () => {});

  it("should sign in", () => {});
});
